# MiniFlow API

FastAPI backend for MiniFlow.

## Local Commands

```bash
uv sync
uv run uvicorn app.main:app --reload
uv run pytest
uv run ruff check .
uv run alembic upgrade head
uv run python seed.py
```

